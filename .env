# KAAL Bridge server URL
# Leave VITE_KAAL_URL unset — production defaults to Vercel serverless /api
# For local self-hosted bridge, create .env.local (not committed) with:
# VITE_KAAL_URL=http://localhost:8369
